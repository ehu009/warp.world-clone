<%= form_with(model: warp_user_level, local: true) do |form| %>
  <% if warp_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(warp_level.errors.count, "error") %> prohibited this warp_level from being saved:</h2>

      <ul>
      <% warp_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
